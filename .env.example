# 數獨小學堂 — 環境變數範例
# 複製成 .env 只給本機參考用；.env 已被 .gitignore 排除，正式值請放在平台的
# 環境變數／repository variables，不要提交進 Git。

# 前端要連的遊戲伺服器位址。唯一的設定入口是 public/js/config.js，
# 部署時由 scripts/inject-server-url.js 從這個變數注入。
# 留空 = 單機模式（目前預設；不需要伺服器也能完整遊玩）。
# 本機測試：node scripts/inject-server-url.js --allow-local http://localhost:3010
GAME_SERVER_URL=

# 伺服器端：允許跨來源存取 /health 的前端網址，逗號分隔；* 表示不限制。
# 例：GAME_ALLOWED_ORIGIN=https://<帳號>.github.io
GAME_ALLOWED_ORIGIN=*

# 伺服器端：監聽的埠與介面（Render 之類的平台會自動注入 PORT）
PORT=3010
HOST=0.0.0.0
